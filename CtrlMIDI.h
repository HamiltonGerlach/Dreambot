
const int channelMIDI = 1;

/*
  if (button1.fallingEdge()) {
    usbMIDI.sendNoteOn(60, 99, channelMIDI);  // 60 = C4
  }
  // Note Off messages when each button is released
  if (button1.risingEdge()) {
    usbMIDI.sendNoteOff(60, 0, channelMIDI);  // 60 = C4
  }
*/
